import test from "node:test";import assert from "node:assert/strict";import {noticeHtml} from "../src/accessibleHtml.js";import {personalize} from "../src/preferences.js";import {retry,ready} from "../src/recovery.js";const notice={id:"1",title:"Urgent cleanup",body:"Register by September 12, 2030.",source:{name:"Demo",url:"https://example.test"},publishedAt:"2030-01-01",tags:["volunteer"]};test("personalization scores interests",()=>assert.equal(personalize([notice],{includeTags:["volunteer"],excludeTags:[],maxItems:5})[0].score,5));test("accessible HTML escapes content and labels article",()=>{const html=noticeHtml({...notice,title:"<Cleanup>"});assert.match(html,/aria-labelledby/);assert.doesNotMatch(html,/<Cleanup>/)});test("retry uses exponential delay",()=>{const p=retry({payload:{},attempts:1,nextAttemptAt:0,lastError:""},new Error("offline"),100);assert.equal(p.nextAttemptAt,2100);assert.deepEqual(ready([p],2100),[p])});
